# OpenClaw Config Atlas Worklog

## 2026-03-08 11:55 CST

- 重大方向调整：界面不再承担配置修改、保存、重启、拖拽删除等写操作，回到“只读 + 备份 + 导出”的定位。
- 保留龙虾画布，但把说明重点切回结构理解：头胸部看 defaults / gateway / tools / channels.defaults，腹节看正式 agent，连线看 bind，肢体里的机器人卡和会话卡看账号、群聊、私聊入口。
- 渠道关键布尔项继续显式显示 `true / false`，避免把“关闭”和“未填写”混淆。
- 渠道字段映射补入了 `qqbot` 的常见凭据字段，便于只读展示时不漏掉现网结构。
- 便携版打包脚本 `package-release.sh` 仍保留，输出目录仍是项目内 `dist/`。

## 当前状态

- `public/app.js`：主入口已退回只读模式；保留读取、备份、导出；龙虾画布按结构层 / agent / bind / 机器人 / 会话来解释。
- `public/styles.css`：现有龙虾画布、大卡片和备份区样式继续沿用。
- `README.md`：需要与只读定位保持一致。
- 下一步如果继续做：优先补只读信息密度，例如更完整的机器人字段展示、bind 命中解释、以及真实浏览器联调截图。
